/**
 * 
 * {
 *  el:"#container",
 *  speed:100（默认），
 * direction:left(默认)，
 * data:['哈哈哈哈哈','快快快看']
 * }
 *
 *
 * @param {*} option
 */

function run(option){
    let el=option.el;
    let speed= '50'||option.speed;
    let direction='left'|| option.direction;
    let data=option.data;
    

}
run()
